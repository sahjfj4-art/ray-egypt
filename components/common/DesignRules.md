# 📋 قواعد التصميم الثابتة لمنصة "راي"

## 🚨 قواعد لا يمكن خرقها أبداً

### 💰 العملة
- **العملة الوحيدة المسموح بها**: الجنيه المصري (ج.م)
- **الرمز**: ج.م أو جنيه
- **التنسيق**: 1,250 ج.م (دائماً باللغة العربية)
- **ممنوع**: دولار، ريال، أي عملة أخرى

### 🎨 الألوان
- **اللون الأساسي**: أزرق رأي (#3b82f6)
- **اللون الثانوي**: ذهبي رأي (#f59e0b)
- **الألوان المحايدة**: درجات الرماد فقط
- **ممنوع**: أي ألوان زاهية أخرى غير معتمدة

### 📝 الخطوط
- **الخط العربي**: Tajawal ثم Cairo
- **الخط الإنجليزي**: Inter
- **ممنوع**: أي خطوط أخرى

### 🏪 الفئات
- **9 فئات فقط**: مطاعم، تسوق، خدمات، صحة، جمال، تعليم، ترفيه، عقارات، سيارات
- **ترتيب ثابت**: لا يمكن تغيير الترتيب
- **أيقونات محددة**: لكل فئة أيقونتها الخاصة

### 🏢 أنواع الأعمال
- **12 نوع فقط**: مطعم، متجر، صيدلية، عيادة، نادي، صالون، عقارات، سيارات، خدمات، مغسلة، ملابس، مقاولات
- **لون لكل نوع**: ثابت ولا يتغير
- **ميزات محددة**: لكل نوع ميزاته الخاصة

### 📐 المسافات
- **المسافات المعتمدة فقط**: xs(4px), sm(8px), md(16px), lg(24px), xl(32px), 2xl(48px), 3xl(64px), 4xl(96px), 5xl(128px)
- **ممنوع**: أي مسافات أخرى

### 🔘 الزوايا
- **الزوايا المعتمدة فقط**: none, sm(2px), base(4px), md(6px), lg(8px), xl(12px), 2xl(16px), 3xl(24px), full
- **ممنوع**: أي زوايا أخرى

### 🌟 الظلال
- **الظلال المعتمدة فقط**: sm, base, md, lg, xl, 2xl, ray
- **ظل ray خاص**: يستخدم للعناصر المهمة فقط

### 📱 نقاط التوقف
- **المعتمدة فقط**: sm(640px), md(768px), lg(1024px), xl(1280px), 2xl(1536px)
- **ممنوع**: أي نقاط توقف أخرى

## ✅ قواعد التنفيذ

### 1. استيراد دائماً من DesignSystem
```typescript
import { RAY_DESIGN_SYSTEM, rayUtils } from './common/DesignSystem';
```

### 2. تنسيق الأسعار دائماً بالدالة المساعدة
```typescript
const price = rayUtils.formatPrice(1250); // "1,250 ج.م"
```

### 3. استخدام الألوان من النظام
```typescript
const color = RAY_DESIGN_SYSTEM.colors.primary[500];
```

### 4. استخدام الفئات من النظام
```typescript
const category = RAY_DESIGN_SYSTEM.categories.food;
```

### 5. استخدام أنواع الأعمال من النظام
```typescript
const businessType = RAY_DESIGN_SYSTEM.businessTypes.restaurant;
```

## 🎯 التحقق التلقائي

قبل كل commit، تأكد من:
- [ ] جميع الأسعار بالجنيه المصري
- [ ] جميع الألوان من النظام المعتمد
- [ ] جميع المسافات من النظام المعتمد
- [ ] جميع الزوايا من النظام المعتمد
- [ ] جميع الخطوط من النظام المعتمد

## ⚡ نصائح سريعة

1. **لا تستخدم قيم عشوائية أبداً**
2. **لا تستخدم ألوان غير معتمدة**
3. **لا تستخدم عملات غير الجنيه**
4. **لا تستخدم مسافات غير معتمدة**
5. **لا تستخدم زوايا غير معتمدة**

## 🔧 أدوات المساعدة

استخدم دوال rayUtils دائماً:
- `rayUtils.formatPrice()` لتنسيق الأسعار
- `rayUtils.getCategoryColor()` للحصول على لون الفئة
- `rayUtils.getBusinessTypeInfo()` للحصول على معلومات العمل

---

**تذكر**: هذه القواعد تضمن هوية موحدة واحترافية للمنصة. الخروج عنها يضر بالهوية البصرية والتجربة المستخدم.
